import { StyleSheet } from "react-native";

const newPageStyles = StyleSheet.create({
  txt: {
    textAlign: "center",
    fontSize: 25,
  },
  container: {
    height: 150,
    alignItems: "center",
  },
  image: {
    width: 300,
    height: 300,
  },
});
export default newPageStyles;
